<ul class="list-inline text-center">
  <% ["valentine", "matthieu"].each do |member| %>
    <li>
      <%= image_tag "#{member}.jpeg", class: "avatar-large" %>
      <h3><%= member.capitalize %></h3>
    </li>
  <% end %>
</ul>
